import { createRootRoute } from "@tanstack/react-router";

/* This code snippet is creating a root route wrapper using the `createRootRoute` function from the
`@tanstack/react-router` library. The `createRootRoute` function is used to define a top-level route
that serves as a layout-less wrapper for other routes in a React application. */
export const rootRouteWrapper = createRootRoute({});
